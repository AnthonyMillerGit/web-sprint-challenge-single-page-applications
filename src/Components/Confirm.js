import React from 'react';
import styled from 'styled-components'

function Confirm() {
    return (
        <div>
            <div>
                <h1>Order Confirmed!</h1>
            </div>
            <div>
                <h2>Hello I just realized i messed up by not writing all of my stuff i need as props for this in the parent folder to this. instead, like an idiot, i wrote all of that stuf in Form.js instead of App.js. so now all of that logic i was supposed to render here can not get passed to this component.And now i have no time to fix such a large issue. Dang it!</h2>
            </div>
        </div>
    )
}

export default Confirm